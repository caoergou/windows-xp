export { createScopedHandle, AgentDeniedError, AgentRateLimitError } from './scopedHandle';
export type {
  ScopedXPHandle,
  ScopedDescription,
  ScopedWindowInfo,
  ScopedDesktopItem,
} from './scopedHandle';
export { expandCaps, DANGEROUS_CAPS, CAP_GROUPS } from './types';
export type { Capability, ScopedHandleConfig, DeniedPolicy } from './types';
