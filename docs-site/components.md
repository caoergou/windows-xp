---
title: Component gallery
---

# Component gallery

The engine _is_ the demo. Below is the live component gallery — every shared XP
primitive (buttons, dialogs, menus, tabs, tooltips, balloons, the status bar…)
rendered by the real package, not a screenshot. It's the same gallery that ships
at [`/windows-xp/gallery/`](/windows-xp/gallery/).

<ClientOnly>
<iframe
  src="/windows-xp/gallery/"
  title="Windows XP component gallery"
  loading="lazy"
  style="width:100%;height:80vh;border:1px solid var(--vp-c-divider);border-radius:8px;background:#fff"
></iframe>
</ClientOnly>

Prefer it full-screen? Open [the standalone gallery](/windows-xp/gallery/).

For the standalone primitives you can import directly (without the full
desktop), see [Subpath imports & primitives](/guide/subpaths).
