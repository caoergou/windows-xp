---
title: "接口：LoginBranding"
---

[@caoergou/windows-xp](/windows-xp/docs/zh/api/index.md) / [index](/windows-xp/docs/zh/api/index/index.md) / LoginBranding

# 接口：LoginBranding

定义于：src/branding.ts:23

登录界面品牌定制。扩展顶层 `avatar`/`username` props。

## 属性

### background?

&gt; `optional` **background?**: `string`

定义于：src/branding.ts:25

屏幕的 CSS 背景：颜色、渐变或图片 URL。

---

### title?

&gt; `optional` **title?**: `string`

定义于：src/branding.ts:27

替换“Microsoft Windows XP”的文字标识。

---

### userName?

&gt; `optional` **userName?**: `string`

定义于：src/branding.ts:31

用户显示名（`username` 的别名，作用域限定在登录界面）。

---

### userTile?

&gt; `optional` **userTile?**: `string`

定义于：src/branding.ts:29

用户头像图片（`avatar` 的别名，作用域限定在登录界面）。
