[@caoergou/windows-xp](/windows-xp/docs/zh/api/index.md) / [index](/windows-xp/docs/zh/api/index/index.md) / CultureKey

# Type Alias: CultureKey

&gt; **CultureKey** = `"en"` \| `"zh"`

Defined in: src/data/culture/types.ts:124

The two built-in culture buckets. Used only for the built-in en/zh content
maps (start menu, browser, IE favorites) — NOT for filtering a package's own
items (see filterByLocale, which matches the real locale).
